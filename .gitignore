.venv/
__pycache__/
*.egg-info/
*.db
*.db-journal
*.db-wal
*.db-shm
# the built frontend (frontend/ → npm run build → served by the package)
src/agentslate/static/
frontend/node_modules/
frontend/dist/
.DS_Store

# a filled-in env file belongs on the server, not in the repo
slate.env
